language: node_js
node_js:
  - "8.11"

branches:
  only:
    - master

before_script:
  - npm install -g firebase-tools
  - npm install -g @angular/cli

script:
  - echo "Building Angular project"
  - ng build --prod

after_success:
  - firebase deploy --project echangenc --token $FIREBASE_TOKEN

notifications:
  email:
    on_failure: change
    on_success: change
